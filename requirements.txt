# Core runtime dependencies (mirrors pyproject.toml)
numpy>=1.26
astropy>=6.0
scipy>=1.11
scikit-image>=0.22
astroalign>=2.6
rich>=13.7

# GUI support (install PySide6 only if you need the desktop interface)
PySide6>=6.7

# Optional GPU runtime for Near star detection (Linux + NVIDIA CUDA 12.x)
# Safe behavior: if unavailable at runtime, ZeSolver falls back to CPU.
cupy-cuda12x; platform_system == "Linux"
nvidia-cuda-runtime-cu12; platform_system == "Linux"
nvidia-cuda-nvrtc-cu12; platform_system == "Linux"
