# Core runtime dependencies (mirrors pyproject.toml)
numpy>=1.26
astropy>=6.0
scipy>=1.11
scikit-image>=0.22
astroalign>=2.6
rich>=13.7

# GUI support (install PySide6 only if you need the desktop interface)
PySide6>=6.7

# Development/test support used by the repository test and diagnostic suite
pytest>=7.0
pytest-cov>=4.0
mypy>=1.8
threadpoolctl>=3.6,<4

# Optional GPU runtime for Near star detection (Linux + NVIDIA CUDA 12.x).
# Do not install by default: CPU fallback is supported and keeps a fresh venv small.
# For GPU machines, install the optional project extra instead:
#   python -m pip install -e ".[gpu]"
